---
title: Capitolo 5-GUIX visualizzare i driver
description: I driver di visualizzazione GUIX definiscono l'interfaccia software tra l'area di disegno astratta e l'hardware di visualizzazione fisico.
author: philmea
ms.author: philmea
ms.date: 05/19/2020
ms.topic: article
ms.service: rtos
ms.openlocfilehash: 7c8f509aee1892693c22f8cfba3207158de4a1e9
ms.sourcegitcommit: e3d42e1f2920ec9cb002634b542bc20754f9544e
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 03/22/2021
ms.locfileid: "104823078"
---
# <a name="chapter-5---guix-display-drivers"></a>Capitolo 5-GUIX visualizzare i driver

I driver di visualizzazione GUIX definiscono l'interfaccia software tra l'area di disegno astratta e l'hardware di visualizzazione fisico. Il driver di visualizzazione GUIX implementa le funzioni di disegno di livello più basso che modificano effettivamente le informazioni sui colori dei pixel nella memoria Canvas e trasferiscono la memoria Canvas al buffer del frame di visualizzazione fisico nei sistemi con doppio buffer.

I driver di visualizzazione GUIX sono definiti da una struttura che contiene i parametri di visualizzazione fisici e un set di puntatori a funzione per le funzioni di driver di basso livello. Usando questi puntatori a funzione indiretta, le funzioni abstract Canvas e widget Drawing vengono rese completamente indipendenti dai dettagli dell'hardware.

GUIX fornisce un set di funzioni di disegno predefinito completo e completamente funzionante per ogni formato di colore e profondità dei colori supportato. Quando si implementa un driver di visualizzazione senza funzionalità di accelerazione hardware specifiche o altre considerazioni specifiche dell'hardware, queste funzioni di disegno predefinite sono in genere sufficienti per l'implementazione del driver finale. Per questi driver più semplici, l'unica funzione che in genere deve essere implementata nel software driver è una funzione per la configurazione del dispositivo hardware. Questa operazione comporta spesso l'inizializzazione di diversi registri hardware per definire il clock di visualizzazione LCD, le dimensioni di visualizzazione e così via. Per tutte le altre funzioni, l'implementazione del driver consente semplicemente di inizializzare i puntatori a funzione GX_DISPLAY sulle implementazioni di funzione predefinite per la profondità e il formato dei colori desiderati.

Quando si implementa un driver di visualizzazione personalizzato, la procedura consigliata consiste nel inizializzare i puntatori della funzione di disegno del driver di visualizzazione con l'implementazione del software predefinita per la profondità di colore che si desidera supportare, quindi sostituire i puntatori a funzione quando si desidera chiamare le implementazioni di funzioni personalizzate (se presenti). Per semplificare questa operazione, è disponibile una funzione di installazione predefinita per ogni profondità e formato di colore supportati. Se, ad esempio, si scrive un driver di visualizzazione RGB a 16 bit 5:6:5, la prima cosa che il driver personalizzato normalmente farebbe è richiamare la routine di installazione generica per questa profondità di colore:

My_custom_565_display_driver UINT (GX_DISPLAY * display)

```c
{
      
      // perform standard function pointer setup
      
      _gx_display_driver_565rgb_setup(display, GX_NULL,
      
             my_buffertoggle);

}
```

Il parametro my_buffer_toggle sopra riportato è un puntatore alla funzione di attivazione/disattivazione del buffer del driver di visualizzazione (che può essere GX_NULL se il driver è a buffer singolo e viene disegnato direttamente nel buffer dei frame hardware).

Se si sta scrivendo un driver di visualizzazione personalizzato, è necessario includere il file di intestazione gx_display. h nell'origine del driver personalizzata, ovvero un file di intestazione di utilizzo interno non disponibile per il software a livello di applicazione.

Le funzioni di disegno del livello di visualizzazione GUIX ricevono come input un puntatore a una struttura **GX_DRAW_CONTEXT** . La struttura **GX_DRAW_CONTEXT** definisce le coordinate di ritaglio per l'operazione di disegno corrente insieme al pennello e ai colori utilizzati. Ogni funzione di disegno riceve come parametri aggiuntivi di input specifici per i requisiti della funzione.

La firma del punto di ingresso del driver **GX_DISPLAY** è definita come

```c
UINT <device>_graphics_driver_<format>(GX_DISPLAY *diplay)
```

Sebbene il nome di questa funzione sia completamente attivo per l'implementatore, la convenzione per i driver forniti con GUIX consiste nell'usare un nome di dispositivo specifico dell'hardware nel <device> campo e nel formato colore per il <format> campo precedente.

Questa funzione deve inizializzare la struttura di **GX_DISPLAY** fornita come input ed eseguire qualsiasi configurazione hardware richiesta. La struttura **GX_DISPLAY** contiene i campi seguenti.

| &nbsp; |
| --- | --- |
| Gx_display_id ULONG <br/> Si tratta di un campo per l'utilizzo da parte dell'applicazione, nei casi in cui viene creata più di un'istanza di un determinato driver. |
| CHAR * gx_display_name <br/> Nome facoltativo utilizzato per identificare il driver. |
| GX_DISPLAY * gx_display_created_next <br/> Questo campo viene inizializzato da GUIX e viene usato per creare e gestire un elenco di tutte le istanze di GX_DISPLAY. |
| GX_DISPLAY * gx_display_created_previous <br/> Questo campo viene inizializzato da GUIX e viene usato per creare e gestire un elenco di tutte le istanze di GX_DISPLAY. |
| GX_VALUE gx_display_color_format <br/> Questo campo deve riflettere il formato dati di grafica supportato da questo driver. I tipi di formato colori vengono definiti nel file di intestazione gx_api. h. |
| GX_VALUE gx_display_width <br/> Questo campo deve essere inizializzato in modo da contenere lo spessore di visualizzazione fisico, in pixel. |
| GX_VALUE gx_display_height <br/> Questo campo deve essere inizializzato in modo da contenere l'altezza di visualizzazione fisica, in pixel. |
| GX_COLOR * gx_display_color_table <br/> Si tratta di un puntatore a una tabella utilizzata per convertire i valori di ID colore in valori di colore specifici del formato colori. |
| GX_PIXELMAP * gx_display_pixelmap_table <br/> Si tratta di un puntatore alla tabella Pixelmap attiva per questa visualizzazione. |
| GX_FONT * gx_display_font_table <br/> Si tratta di un puntatore alla tabella dei tipi di carattere attiva per questa visualizzazione. |
| GX_COLOR * gx_display_palette <br/> Per i driver in modalità tavolozza, si tratta di un puntatore alla tavolozza dei colori attiva. Per i driver che non utilizzano una tavolozza dei colori, questo puntatore è GX_NULL. |
| Gx_display_pixelmap_table_size UINT <br/> Numero di voci nella tabella Pixelmap attiva. |
| Gx_display_font_table_size UINT <br/> Numero di voci nella tabella dei tipi di carattere attiva. |
| Gx_display_palette_size UINT <br/> Numero di voci nella tavolozza dei colori (se presente). |
| Gx_display_handle ULONG <br/> Identificatore univoco, o *handle*, che specifica la visualizzazione.
| Gx_display_driver_ready UINT <br/> Questo campo viene usato per segnalare a GUIX quando il driver è pronto per l'operazione. In alcuni casi, è possibile che il driver richieda diversi livelli di inizializzazione e configurazione, nel qual caso GUIX non deve tentare di utilizzare il driver. Questo flag deve essere impostato su 1 quando il driver è pronto per soddisfare le richieste di disegno. |
| VOID * gx_display_driver_data <br/> Questo campo è utilizzato dall'implementazione del driver. Se il driver deve creare e fare riferimento a informazioni aggiuntive non disponibili nella struttura di GX_DISPLAY, il driver deve allocare spazio per e puntare a questi dati aggiuntivi usando questo campo della struttura. Un esempio di dati aggiuntivi specifici del driver potrebbe includere il canale DMA usato dal driver o dal canale SPI a cui è connesso il buffer del frame di visualizzazione. |
| VOID (* gx_display_driver_drawing_initiate) (struct GX_DISPLAY_STRUCT * display, struct GX_CANVAS_STRUCT * Canvas) <br/> Si tratta di un puntatore a funzione che, se non NULL, viene richiamato dalla funzione gx_canvas_drawing_initiate. Per i driver di visualizzazione che utilizzano un elenco di elementi grafici di accelerazione grafica o hardware, questa funzione può essere utilizzata per iniziare un nuovo elenco di visualizzazione. Questo puntatore a funzione può essere NULL. |
| VOID (* gx_display_driver_palette_set) (struct GX_DISPLAY_STRUCT * display, GX_COLOR * palette, INT Count) <br/> Si tratta di un puntatore a una funzione per installare una tavolozza dei colori. Questa funzione è NULL a meno che il driver non funzioni in modalità tavolozza (detta anche tabella di ricerca dei colori o CLUT). |
| VOID (* gx_display_driver_simple_line_draw) (GX_DRAW_CONTECT * context, INT x1, INTy1, INT X2, INT Y2) <br/> Si tratta di un puntatore a una funzione per implementare il disegno di righe generico, senza anti-aliasing. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_simple_wide_line_draw) (GX_DRAW_CONTECT * context, INT x1, INTy1, INT X2, INT Y2) <br/> Si tratta di un puntatore a una funzione che consente di implementare il disegno a linee Wide generico, senza anti-aliasing. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_anti_aliased_line_draw) (GX_DRAW_CONTECT * context, INT x1, INTy1, INT X2, INT Y2) <br/> Si tratta di un puntatore a una funzione per implementare il disegno a linee anti-alias generico. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_anti_aliased_wide_line_draw) (GX_DRAW_CONTECT * context, INT x1, INTy1, INT X2, INT Y2) <br/> Si tratta di un puntatore a una funzione per implementare un disegno generico con anti-aliasing della linea. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_horizonal_line_draw) (GX_DRAW_CONTECT * context, INT x1, INT X2, INT y) <br/> Si tratta di un puntatore a una funzione per implementare il caso speciale del disegno a linee orizzontali. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_horizonal_pixelmap_line_draw) (GX_DRAW_CONTECT * context, INT x1, INT X2, INT y, GX_PIXELMAP * map) <br/> Si tratta di un puntatore a una funzione per implementare il disegno di una singola riga di Pixelmap. Questa funzione viene utilizzata internamente per le forme di riempimento del criterio. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_vertical_line_draw) (GX_DRAW_CONTECT * context, INT Y1, INT Y2, INT x) <br/> Si tratta di un puntatore a una funzione per implementare il caso speciale del disegno a linee orizzontali. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_horizonal_pattern_line_draw) (GX_DRAW_CONTECT * context, INT x1, INT X2, INT y) <br/> Si tratta di un puntatore a una funzione per implementare il disegno orizzontale della linea del modello. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_vertical_pattern_line_draw) (GX_DRAW_CONTECT * context, INT Y1, INT Y2, INT x) <br/> Si tratta di un puntatore a una funzione per implementare il disegno verticale della linea del modello. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_canvas_copy) (struct GX_CANVAS_STRUCT * source, struct GX_CANVAS_STRUCT * dest) <br/> Si tratta di un puntatore a una funzione per copiare i dati dell'area di disegno da un'area di disegno a un'altra. Il rettangolo dell'area di disegno di origine non valido viene usato per definire l'area di copia. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_canvas_blend) (struct GX_CANVAS_STRUCT * source, struct GX_CANVAS_STRUCT * dest) <br/> Si tratta di un puntatore a una funzione per l'alfa-blend dei dati Canvas dall'area di disegno di origine con i dati esistenti nell'area di disegno di destinazione. Il rettangolo dell'area di disegno di origine non valido viene usato per definire l'area di Blend. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_pixelmap_blend) (GX_DRAW_CONTEXT * context, INT XPos, INT YPos, GX_PIXELMAP * PMP, GX_UBYTE Alpha) <br/> Si tratta di un puntatore a una funzione per la fusione di un Pixelmap nell'area di disegno dello sfondo definito dal contesto di disegno. Il valore alfa fornito può essere aggiunto a un canale alfa contenuto nei dati Pixelmap. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_pixelmap_draw) (GX_DRAW_CONTEXT * context, INT XPos, INT YPos, GX_PIXELMAP * PMP) <br/> Si tratta di un puntatore a una funzione che consente di creare un Pixelmap nell'area di disegno definita dal contesto di disegno. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_jpeg_draw) (GX_DRAW_CONTEXT * context, INT XPos, INT YPos, GX_PIXELMAP * PMP) <br/> Si tratta di un puntatore a una funzione che consente di decodificare un'immagine jpg e di eseguirne il rendering direttamente nell'area di disegno. Questa funzione viene fornita solo se è stato definito GX_SOFTWARE_DECODER_SUPPORT. Il puntatore a questa funzione è NULL. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_png_draw) (GX_DRAW_CONTEXT * context, INT XPos, INT YPos, GX_PIXELMAP * PMP) <br/> Si tratta di un puntatore a una funzione per decodificare un'immagine png ed eseguirne il rendering direttamente nell'area di disegno. Questa funzione viene fornita solo se è stato definito GX_SOFTWARE_DECODER_SUPPORT. Il puntatore a questa funzione è NULL. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_pixelmap_rotate) (GX_DRAW_CONTEXT * context, INT XPos, INT YPos, GX_PIXELMAP * PMP INT Angle, INT rot_cx, INT rot_cy) <br/> Si tratta di un puntatore a una funzione per ruotare un pixemap ed eseguire il rendering del risultato direttamente nell'area di disegno. Questa funzione viene richiamata dal gx_canvas_pixelmap_rotate implementazioni APIDefault di questa funzione sono fornite per ogni formato di colore e profondità di colore supportati. |
| VOID * gx_display_driver_pixel_write) (GX_DRAW_CONTEXT * context, INT x, INT y, GX_COLOR Color) <br/> Si tratta di un puntatore a una funzione per scrivere un pixel nella memoria dell'area di disegno. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. <br/>
| VOID * gx_display_driver_block_move) (GX_DRAW_CONTEXT * context, GX_RECTANGLE * Block, INT xshift, INT yshift) <br/> Si tratta di un puntatore a una funzione per spostare o spostare un blocco di pixel all'interno di un'area di disegno. Questa funzione viene utilizzata principalmente per scorrere rapidamente il contenuto di una finestra. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_pixel_blend) (GX_DRAW_CONTEXT * context, INT x, INT y, GX_COLOR color, GX_UBYTE Alpha) <br/> Questa funzione viene utilizzata per la fusione alfa del valore del colore pixel in ingresso con il valore di colore esistente nella memoria Canvas nella posizione x, y. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| GX_COLOR (* gx_display_driver_native_color_get) (GX_COLOR rawcolor) <br/> Questa funzione converte un colore dal formato di colore A:R: G:B a 32 bit usato internamente da GUIX al formato di colore nativo dell'area di disegno e alla visualizzazione. È prevista una perdita di informazioni sui colori per i driver di visualizzazione in esecuzione con profondità del colore inferiori. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| USHORT (* gx_display_driver_row_pitch_get) (larghezza USHORT) <br/> Restituisce il numero di byte o lo stride di una riga di dati grafici in base alla larghezza dell'area di disegno richiesta. Questa funzione viene usata per calcolare le dimensioni dell'area di memoria necessaria per creare un'area di disegno. L'altezza e la larghezza delle righe non sono sempre le stesse a causa dei vincoli di allineamento della riga di analisi hardware. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_buffer_toggle) (struct GX_CANVAS_STRUCT * Canvas, GX_RECTANGLE * dirty_area) <br/> Si tratta di un puntatore a una funzione per passare tra i buffer dei frame di lavoro e visibili per i sistemi di memoria con doppio buffer. Questa funzione deve prima indicare all'hardware di iniziare a usare il nuovo buffer dei frame, quindi copiare la parte modificata del nuovo buffer visibile nel buffer complementare per assicurarsi che i due buffer siano sincronizzati. | 
| VOID (* gx_display_driver_polygon_draw) (GX_DRAW_CONTEXT * context, INT num_points, GX_POINT * vertici <br/> Puntatore a una funzione per creare un poligono. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_polygon_fill) (GX_DRAW_CONTEXT * context, INT num_points, GX_POINT * vertici <br/> Puntatore a una funzione per creare un poligono pieno. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_circle_draw) (GX_DRAW_CONTEXT * context, INT XCENTER, INT ycenter, UINT r) <br/> Puntatore a una funzione per creare un cerchio. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_anti_aliased_circle_draw) (GX_DRAW_CONTEXT * context, INT XCENTER, INT ycenter, UINT r) <br/> Puntatore a una funzione per creare un cerchio con anti-aliasing. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_wide_circle_draw) (GX_DRAW_CONTEXT * context, INT XCENTER, INT ycenter, UINT r) <br/> Puntatore a una funzione per disegnare un cerchio con un contorno ampio. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_wide_anti_aliased_circle_draw) (GX_DRAW_CONTEXT * context, INT XCENTER, INT ycenter, UINT r) <br/> Puntatore a una funzione per disegnare un cerchio con anti-aliasing con un contorno ampio. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_circle_fill) (GX_DRAW_CONTEXT * context, INT XCENTER, INT ycenter, UINT r) <br/> Puntatore a una funzione per creare un cerchio pieno. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_arc_draw) (GX_DRAW_CONTEXT * context, INT XCENTER, INT ycenter, UINT r, INT start_angle, INT end_angle) <br/> Puntatore a una funzione per creare un arco. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_anti_aliased_arc_draw) (GX_DRAW_CONTEXT * context, INT XCENTER, INT ycenter, UINT r, INT start_angle, INTend_angle) <br/> Puntatore a una funzione per creare un arco con anti-aliasing. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_wide_arc_draw) (GX_DRAW_CONTEXT * context, INT XCENTER, INT ycenter, UINT r, INT start_angle, INT end_angle) <br/> Puntatore a una funzione per disegnare un arco con un contorno ampio. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_anti_aliased_wide_arc_draw) (GX_DRAW_CONTEXT * context, INT XCENTER, INT ycenter, UINT r, INT start_angle, INTend_angle) <br/> Puntatore a una funzione per creare un arco con anti-aliasing. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_arc_fill) (GX_DRAW_CONTEXT * context, INT XCENTER, INT ycenter, UINT r, INT start_angle, INT end_angle) <br/> Puntatore a una funzione per creare un arco pieno. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_pie_fill) (GX_DRAW_CONTEXT * context, INT XCENTER, INT ycenter, UINT r, INT start_angle, INT end_angle) <br/> Puntatore a una funzione per creare una torta piena. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_ellipse_draw) (GX_DRAW_CONTEXT * context, INT XCENTER, INT ycenter, INT a, INT b) <br/> Puntatore a una funzione per creare un'ellisse. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_anti_aliased_ellipse_draw) (GX_DRAW_CONTEXT * context, INT XCENTER, INT ycenter, INT a, INT b) <br/> Puntatore a una funzione per creare un'ellisse. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_wide_ellipse_draw) (GX_DRAW_CONTEXT * context, INT XCENTER, INT ycenter, INT a, INT b) <br/> Puntatore a una funzione per disegnare un'ellisse con un contorno ampio. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_anti_aliased_wide_ellipse_draw) (GX_DRAW_CONTEXT * context, INT XCENTER, INT ycenter, INT a, INT b) <br/> Puntatore a una funzione per disegnare un'ellisse con un contorno ampio. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_ellipse_fill) (GX_DRAW_CONTEXT * context, INT XCENTER, INT ycenter, INT a, INT b) <br/> Puntatore a una funzione per creare un'ellisse piena. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_8bit_glyph_draw) (GX_DRAW_CONTEXT * context, GX_RECTANGLE * draw_area, GX_POINT * map_offset, constGX_GLYPH * Glyph) <br/> Puntatore a funzione per disegnare il glifo di testo con alias a 1 8 bit nell'area di disegno usando il pennello del contesto di disegno corrente. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_4bit_glyph_draw) (GX_DRAW_CONTEXT * context, GX_RECTANGLE * draw_area, GX_POINT * map_offset, const GX_GLYPH * Glyph) <br/> Puntatore a funzione per disegnare il glifo di testo con alias a 1 4 bit nell'area di disegno usando il pennello del contesto di disegno corrente. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |
| VOID (* gx_display_driver_1bit_glyph_draw) (GX_DRAW_CONTEXT * context, GX_RECTANGLE * draw_area, GX_POINT * map_offset, const GX_GLYPH * Glyph) <br/> Puntatore a funzione per disegnare un glifo di testo monocromatico a 1 1 bit nell'area di disegno usando il pennello del contesto di disegno corrente. Le implementazioni predefinite di questa funzione sono fornite per ogni formato di colore e profondità di colore supportato. |


